## rewrite_share综合流
加入了保存和加载AIG网络的命令，魔改了rewrite命令
case1-13，16，17，23，24，25，27，30效果最好，

## 九之星综合流
12，14，18-22，26，28，29效果最好
其中关键命令是
```
&get -nm; &synch2; &choice; &nf; &put;;&get -nm; &nf; &put;;sop; fx; strash; ps
```
映射优化最后在转回AIG
有个很大的劣势：需要去读库文件，内存会上升很多很多

## 目前rewrite_share综合流如下
```
sop;
fx;
strash
aigstore -i 0
strash
balance
rewrite_share -z -K 100 -S 1 -T 2 -M 1 -R 4 -v
rewrite -z
refactor
refactor -z
b -l; rewrite_share -z -K 100 -S 1 -T 2 -M 1 -R 4 -v; rf -l; b -l; rewrite_share -z -K 100 -S 1 -T 2 -M 1 -R 4 -v; rwz -l; b -l; rfz -l; rwz -l; b -l
dch
b -l; rewrite_share -z -K 100 -S 1 -T 2 -M 1 -R 4 -v; rf -l; b -l; rewrite_share -z -K 100 -S 1 -T 2 -M 1 -R 4 -v; rwz -l; b -l; rfz -l; rwz -l; b -l
balance
balance
rewrite_share -z -K 100 -S 1 -T 2 -M 1 -R 4 -v 
rewrite -z
refactor
refactor -z
b -l; rewrite_share -z -K 100 -S 1 -T 2 -M 1 -R 4 -v; rf -l; b -l; rewrite_share -z -K 100 -S 1 -T 2 -M 1 -R 4 -v; rwz -l; b -l; rfz -l; rwz -l; b -l
dch
b -l; rewrite_share -z -K 100 -S 1 -T 2 -M 1 -R 4 -v; rf -l; b -l; rewrite_share -z -K 100 -S 1 -T 2 -M 1 -R 4 -v; rwz -l; b -l; rfz -l; rwz -l; b -l
balance
strash
rewrite
rewrite_share -z -K 100 -S 1 -T 2 -M 1 -R 4 -v
refactor
balance
aigstore -i 1
rewrite; rewrite;&get -nm; &sopb; &put; strash;
aigstore -i 2
aigrestore -a
ps
```


目前魔改的rewrite综合流有个20个优于九之星，这个20个case不论是在九之星综合流还是魔改的rewrite综合流中都是AIG层面的优化（rewrite_share,rewrite,refactor等）或是sop;fx占主要贡献
但是剩下的10个在九之星综合流中都是这段综合流起作用
&get -nm; &synch2; &choice; &nf; &put;;，进行了映射等操作，
九之星的脚本跑出来的case很难再去优化了感觉已经到顶了，但是九之星的需要读库文件，内存和时间直接拉完了，



strash; rewrite;&get -nm; &synch2; &if; &put;;&get -nm; &st; &if; &put;;sop; fx; strash; ps